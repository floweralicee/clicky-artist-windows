# Clicky for Animators — config
# This file ships with the app. It contains no secrets.
VERCEL_API_URL=https://backend-vert-zeta-97.vercel.app
# Optional: lets sign-in work before the Vercel backend is live (public anon key).
SUPABASE_URL=
SUPABASE_ANON_KEY=
